> for links exclude "indian kanoon"

> add a tool that just takes the final_response and just generates a 4 times larger response (4 times more tokens) - only for "detailed mode"

> for citations - user must also be able to know within the final response; which text_part was generated by which reference - so a mapping of { text_start & text_end offset - link } sent with the final response!